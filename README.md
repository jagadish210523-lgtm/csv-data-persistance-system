# csv-data-persistance-system
A lightweight, object-oriented Student Management System built in Python. Implements persistent flat-file CSV storage, CRUD operations via a command-line interface, and structured data handling using Python classes.
